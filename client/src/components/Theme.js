import { createTheme } from "@material-ui/core/styles";
import createMuiStyle from "@material-ui/core/styles/createStyles";

const theme = createTheme({
	palette: {
		primary: {
			main: "#FFFFFFF",
		},
		secondary: {
			main: "#FFFFFFF",
		},
	},
});

export default theme;
